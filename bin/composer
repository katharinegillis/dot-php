#!/usr/bin/env bash

docker run --rm -it -v "$PWD:/app" -w /app --volume $SSH_AUTH_SOCK:/ssh-auth.sock --volume /etc/passwd:/etc/passwd:ro --volume /etc/group:/etc/group:ro --env SSH_AUTH_SOCK=/ssh-auth.sock --user $(id -u):$(id -g) composer "$@"